import { Button } from '@mui/material'
import { styled } from '@mui/system'

export const NavButton = styled(Button)({
    '&.MuiButton-root':{
        color: 'inherit',
        textTransform: 'none',
        fontFamily: 'Poppins'
    }
})